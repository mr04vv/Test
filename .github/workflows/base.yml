name: Base CI

on:
  repository_dispatch:
    types: [echo_message]

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
        - name: Create annotation for build error
        run: echo "hogehoge"

